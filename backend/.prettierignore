# exclude everything by default
/**/*

# But don't exclude folders (i.e. only exclude files)
!/**/*/

# exclude dist and node_modules
dist/
node_modules/

# ignore nexus generated files
app/middleware/graphql/.nexus-generated/

# include ts files
!app/**/*.ts
