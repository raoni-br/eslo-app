/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OnboardingButtonRequiredPipe } from './onboarding-button-required.pipe';

describe('Pipe: OnboardingButtonRequirede', () => {
    it('create an instance', () => {
        let pipe = new OnboardingButtonRequiredPipe();
        expect(pipe).toBeTruthy();
    });
});
