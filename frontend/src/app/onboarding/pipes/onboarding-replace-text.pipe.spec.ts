/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OnboardingReplaceTextPipe } from './onboarding-replace-text.pipe';

describe('Pipe: OnboardingReplaceTexte', () => {
    it('create an instance', () => {
        let pipe = new OnboardingReplaceTextPipe();
        expect(pipe).toBeTruthy();
    });
});
